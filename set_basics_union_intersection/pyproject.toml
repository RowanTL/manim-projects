[project]
name = "set-basics-union-intersection"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "manim>=0.19.0",
]

[tool.uv.extra-build-dependencies]
"manimpango-0.6.1" = ["setuptools"]
